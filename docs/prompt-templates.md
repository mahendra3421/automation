# Prompt Templates

Use these templates to reduce ambiguity before making automated changes.

## Repo improvement request

Copy/paste and fill in:

- Target repo URL:
- Preferred change type (docs / tests / bug fix / refactor / setup):
- Constraints (avoid certain files, languages, frameworks):
- Verification required (tests/build/lint) and how to run it:
- Risk tolerance (low/medium/high):
- Deadline (if any):

## “Daily improvements” mode

- Repos allowed to modify:
- Repos never modify:
- Preferred commit size (tiny / small / medium):
- Preferred workflow (commit to main vs PR first):
- Preferred topics this week (docs, CI, tests, refactors):

## Clarifying questions (quick)

1. Which repo should be improved today?
2. Should changes land directly on `main` or via PRs?
3. Is “documentation-first” preferred unless a bug is obvious?
4. Any areas to avoid (auth, payments, infra)?
